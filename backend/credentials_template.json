{
  "_instructions": [
    "1. Go to https://console.cloud.google.com",
    "2. Create a project (or select an existing one)",
    "3. APIs & Services → Enable APIs → search 'Gmail API' → Enable",
    "4. APIs & Services → Credentials → Create Credentials → OAuth client ID",
    "5. Application type: Desktop app",
    "6. Download JSON → rename it to credentials.json in this directory",
    "7. On first run the browser will open for you to grant access",
    "8. A token.json will be created automatically for subsequent runs"
  ]
}
